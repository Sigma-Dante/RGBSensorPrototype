# RGBSensorPrototype
Arduino Library for the Sensor Prototype.

Follow the pinout instructions labelled in Settings_Modified.h and BLE_UART.cpp

Note that the MUX reset pin is also attached to the Arduino UNO reset pin.
